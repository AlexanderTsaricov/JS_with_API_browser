<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Расписание занятий</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="style/style.css">
</head>
<body class="bg-dark">
    <h1 class="display-3 text-success fw-bold schedule">Расписание занятий</h1>
    <p class="discription text-success">
        Для записи впишите имя и нажмите "записаться", а для отмены записи впишите имя и нажмите "отменить"
    </p>
    <div class="scheduleHeadBox">
        {{#each schedule}}
        <div class="scheduleBox bg-success-subtle border border-success border-4">
            <h3 class="scheduleBox_name text-primary-emphasis">{{this.name}}</h3>
            <p class="scheduleBox_time text-info-emphasis">Начало в {{this.startTime}}, конец в {{this.endTime}}</p>
            <p class="scheduleBox_maxSudents text-danger-emphasis" data-max={{this.maxStudents}}>Максимальное количество участников: {{this.maxStudents}}</p>
            <p class="scheduleBox_registredStudents text-info-emphasis" data-students={{this.coutRegistred}}>Зарегистрировано студентов {{this.coutRegistred}}</p>
            <button class="scheduleBox_button bg-success-subtle btn-outline-success" id="{{this.id}}">Записаться</button>
            <button class="scheduleBox_exitButton bg-success-subtle btn-outline-success" id="{{this.id}}">Отменить</button>
            <input class="scheduleBox_input" type="text" placeholder="Your name">
        </div>
        {{/each}}
    </div>
    <script src="js/index.js"></script>
</body>
</html>